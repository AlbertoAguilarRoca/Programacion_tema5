
public class Buscar {

	/*public static int bubble (Producto[] miArray, String producto) {
		//Nos devuelve el indice (la posición) del elemento si esta dentro del Array
		//Si no esta dentro del array, nos devuelve -1
		//Numero medio de pasos = Log2(N)
		int izq=0;
		int der=miArray.length-1;
		int centro = (izq+der)/2;
		int contador=1;
		while ((izq<=der) && (!miArray[centro].equals(producto))) {
			System.out.println("Número de paso: "+contador);
			contador++;
			if(numero<miArray[centro]) {			//Si el numero buscado esta en la parte izquierda
				der=centro-1;						//Movemos la derecha justo detras del centro
			}else{									//Si el numero buscado esta en la parte derecha
				izq=centro+1;						//Movemos la izquierda justo delante del centro
			}
			centro=(izq+der)/2;						//Recalculamos el centro
		}
		
		if (izq>der) {
			return -1;
		}else {
			return centro;
		}
	}*/

}
